namespace Rehawk.UIFramework
{
    /// <summary>
    /// Specifies the direction of a data binding connection in the UI framework.
    /// </summary>
    public enum BindingConnectionDirection
    {
        SourceToDestination,
        DestinationToSource
    }
}